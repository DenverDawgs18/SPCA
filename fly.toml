# fly.toml — Medina County SPCA
# See https://fly.io/docs/reference/configuration/

app = 'medina-county-spca'
primary_region = 'ord'

[build]
  dockerfile = 'Dockerfile'

[deploy]
  release_command = 'python manage.py migrate --noinput'

[env]
  DEBUG = 'False'
  DJANGO_SETTINGS_MODULE = 'spca_project.settings'
  ALLOWED_HOSTS = 'medina-county-spca.fly.dev'
  CSRF_TRUSTED_ORIGINS = 'https://medina-county-spca.fly.dev'

[[mounts]]
  source = 'spca_media'
  destination = '/app/media'

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [[http_service.checks]]
    grace_period = "60s"
    interval = "15s"
    method = "GET"
    path = "/health/"
    timeout = "10s"
    [http_service.checks.headers]
      Host = "medina-county-spca.fly.dev"

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
